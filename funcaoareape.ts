type parametros = {
    a:number;
    b:number;
   
};


function areat({a,b}:parametros){
    const area = a*b;
    const perimetro = 2*a + 2*b; 

return (calculoarea: area, calculoperimetro: perimetro){

};
}
 
const a = areat({a:2,b:3})
console.log(a)